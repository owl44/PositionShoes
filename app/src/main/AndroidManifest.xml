<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shoes.position">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".config.PSApplicationConfig"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ImageTranslucentTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.mine.MyEquitActivity"
            android:theme="@style/AppThemeNoTitle"/>
        <activity android:name=".ui.mine.StepDataActivity"
            android:theme="@style/AppThemeNoTitle"/>
        <activity android:name=".ui.mine.ProblemActivity" />
        <activity android:name=".ui.mine.AboutActivity" />
        <activity android:name=".ui.mine.SetActivity" />
        <activity android:name=".ui.mine.PersonalInfoActivity"></activity>
    </application>

</manifest>